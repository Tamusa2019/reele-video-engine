# =============================================================================
# Reele Video Engine - Environment Configuration
# =============================================================================

# Database (SQLite for local/Render; Vercel Postgres for Vercel)
DATABASE_URL=file:./db/custom.db

# Upload directory (defaults to ./upload relative to app root)
# UPLOAD_DIR=/home/z/my-project/upload

# Database directory (defaults to ./db)
# DB_DIR=/home/z/my-project/db
